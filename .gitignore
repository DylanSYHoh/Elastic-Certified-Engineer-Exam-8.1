# Snapshot repository data - generated by Elasticsearch, never commit.
# Ignore the CONTENTS, not the directory, so .gitkeep can be tracked and the
# bind-mount target exists on a fresh clone.
backups/*
!backups/.gitkeep

# Local editor / IDE
.idea/
.vscode/
.DS_Store

# Config backups
*.bak
